/**
 * The only file you need to edit before uploading.
 *
 * Both Supabase values below are safe to publish: the anon key is designed to
 * sit in a public page, and row level security in sql/schema.sql is what
 * actually decides who can read and write. Do not paste the service_role key
 * here — that one bypasses every policy.
 *
 * Find them in Supabase under Project Settings -> API.
 */

export const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co'
export const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY'

/** Printed on the certificate and in the page header. */
export const ORGANISATION_NAME = 'Colney & Co'
