# PostgreSQL connection string used by both Prisma CLI and the app at runtime.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/electrical_safety_check?schema=public"

# Shown in the PDF footer and on the certificate header.
NEXT_PUBLIC_ORGANISATION_NAME="Colney & Co"
